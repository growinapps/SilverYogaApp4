<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.growin.silveryogaapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SilverYogaApp">
        <activity android:name=".Profile"></activity>
        <activity android:name=".YogaVideo" />
        <activity android:name=".PartitionYoga" />
        <activity android:name=".FavoriteYoga" />
        <activity android:name=".Login" />
        <activity android:name=".MainActivity2">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ComsetSystem" />
        <activity android:name=".RankingYoga" />
        <activity android:name=".DetailYoga" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>